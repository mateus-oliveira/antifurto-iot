import { StyleSheet } from 'react-native';

export const appStyles = StyleSheet.create({
    background: {
        flex: 1,
    },
    scrollContent: {
        gap: 16,
        paddingBottom: 32,
        paddingHorizontal: 18,
        paddingTop: 12,
    },
});